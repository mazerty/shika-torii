<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="fr.mazerty.shika.torii.UserDao">

    <select id="selectByEmailAndApplication" resultType="fr.mazerty.shika.ishi.User">
        SELECT
            id,
            email,
            password
        FROM torii.t_user u
            INNER JOIN torii.t_user_application ua ON u.id = ua.user_fk
        WHERE email = #{user.email}
              AND ua.application_fk = #{application.id}
    </select>

</mapper>
