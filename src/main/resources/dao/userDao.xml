<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="fr.mazerty.shika.ishi.UserDao">

    <select id="selectByEmailAndApplication" resultType="fr.mazerty.shika.ishi.User">
        SELECT
            u.id,
            u.email,
            u.password
        FROM torii.t_user u
            INNER JOIN torii.t_user_application ua ON u.id = ua.user_fk
            INNER JOIN torii.t_application a ON ua.application_fk = a.id
        WHERE u.email = #{user.email}
              AND a.name = #{application}
    </select>

</mapper>
